import React from 'react';
import UploadForm from '../components/UploadForm';

const Home = (props) => {


  return (
    <div>
      {/* { currentUser ? (
        <> */}
          <UploadForm />
        {/* </>
      ) : (
        <>
        
        </>
      )
      } */}
    </div>
  )
}

export default Home